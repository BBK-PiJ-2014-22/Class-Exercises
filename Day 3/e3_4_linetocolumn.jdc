
println("Enter Text");
String userText = System.console().readLine();

for (int l = 0; l < userText.length(); l++){
	if (userText.charAt(l) != ' '){
		print(userText.charAt(l));
	}else{
		println(userText.charAt(l));
	}	
}
